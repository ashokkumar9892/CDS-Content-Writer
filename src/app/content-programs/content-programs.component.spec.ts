/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ContentProgramsComponent } from './content-programs.component';

describe('Component: ContentPrograms', () => {
  it('should create an instance', () => {
    let component = new ContentProgramsComponent();
    expect(component).toBeTruthy();
  });
});
